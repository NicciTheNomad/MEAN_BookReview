export class Review {
  _id: string;
  // bookID: string;
  name: string;
  stars: number;
  description: string;
  book: string;
  // book: { type: Schema.Types.ObjectId; ref: 'Book'; required: true };

  constructor() {}
}
